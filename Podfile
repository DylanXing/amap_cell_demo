# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'amap_cell_annotation' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

      pod 'AMap3DMap'
      pod 'AMapSearch','~> 8.1.0'
      pod 'AMapLocation'
      pod 'Masonry'
      pod 'YYCategories'

end
